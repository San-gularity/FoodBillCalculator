{
  "name": "food-bill-splitter",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Scan a receipt, add people, tap to assign items, get an exact split.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "start": "node scripts/serve.js",
    "serve": "node scripts/serve.js",
    "config": "node scripts/write-config.mjs"
  }
}
